interface IResponse {}

export class Create{{pascalCase name}}UseCase implements UseCase<{{pascalCase name}}, IResponse> {}

const makeSut = () => {
    const sut = new Create{{pascalCase name}}UseCase();

    return {
        sut,
    }
}

describe("descrição", () => {
      it('deve ser possível ', async () => {
    const { sut } = makeSut();
  });
})