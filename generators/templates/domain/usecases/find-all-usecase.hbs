@Injectable()
export class FindAll{{pascalCase name}} implements UseCase<number, {{pascalCase name}}[]> {
    constructor (
        @Inject(PROVIDER.REPOSITORY)
        private readonly {{dashcase name}}Repository: I{{pascalCase name}}Repository,
    ) {}   

    public async execute(): Promise<{{pascalCase name}}[]> {
        return this.{{dashcase name}}Repository.findAll();
    } 
}