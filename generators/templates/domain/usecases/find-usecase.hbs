@Injectable()
export class Find{{pascalCase name}}UseCase implements UseCase<number, {{pascalCase name}}> {
    constructor (
        @Inject(PROVIDER.REPOSITORY)
        private readonly {{dashcase name}}Repository: I{{pascalCase name}}Repository,
    ) {}   

    public async execute(id: number): Promise<{{pascalCase name}}> {
        return this.{{dashcase name}}Repository.getById(id);
    } 
}