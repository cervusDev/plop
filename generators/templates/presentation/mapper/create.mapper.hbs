import { {{pascalCase name}}Entity } from 'src/domain/{{dashcase name}}/entities/{{dashcase name}}.entity';
import { Mapper } from 'presentation/base/mapper';
import { Create{{pascalCase name}}Dto } from 'presentation/rest/v1/{{dashcase name}}/dto/{{dashcase name}}.dto';

export class Create{{pascalCase name}}Mapper implements Mapper<Create{{pascalCase name}}Dto, {{pascalCase name}}Entity> {}